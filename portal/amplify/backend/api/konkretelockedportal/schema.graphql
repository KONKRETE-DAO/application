type CodeModel @model @auth(rules: [{allow: public, operations: [read]}]) {
  id: ID!
  valu: String
  usageCount: Int
}

type VisitorModel @model @auth(rules: [{allow: public, operations: [read]}]) {
  id: ID!
  email: AWSEmail
}
 